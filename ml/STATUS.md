# ML track status

- 2026-09-17: georakshak_ml B0 ready. Install with `pip install -e ml`, then `from georakshak_ml import score, explain, active_model, MissingFeaturesError`. The model is `b0-rules-0.1.0`, an uncalibrated rule-based heuristic. Required features: `slope_deg_mean`, `relief_m`. See ml/georakshak_ml/README.md. Run tests with `cd ml && .venv/bin/python -m pytest -q`.
- 2026-09-17: Provisional pilot handoff ready in `ml/data/processed/aizawl-mizoram/` (status PROVISIONAL). It contains manifest.json, grid_cells.geojson (2,798 cells), historical_landslides.geojson (19), locations.geojson (103), road_segments.geojson (5,660), boundary.geojson, and rainfall_daily.csv (IMD, 2017-05-15..2017-06-30). Data files are gitignored, so rebuild with `ml/.venv/bin/python ml/pipeline/build_all.py`. See ml/pipeline/README.md and ml/reports/data-spike.md.
- 2026-09-17 (v2): Stage A retrained under a **pre-registered adoption gate** with exposure-controlled labels and leakage-safe spatial CV (`ml/reports/evaluation-2026-09-17-v2.md`). **No trained model was adopted** (RF-T6 reached ROC-AUC 0.747 ± 0.066 but flagged 57-68 % of the pilot at the operating threshold, and did not beat LR by more than LR's fold std). **`georakshak_ml` is unchanged: b0-rules-0.1.0 is still served, `metrics: None`, same interface and required features.**
- 2026-09-17 (v2): GSI Bhukosh retried 7 more times (17:48-17:49 UTC): DNS resolves, every TCP connect to 443/80 timed out (log `ml/data/raw/gsi/gsi_attempts_20260917.tsv`). The inventory in the handoff remains **nasa-glc** (approved fallback S16). `manifest.json` was regenerated with that stated explicitly; `grid_cells.geojson`, `road_segments.geojson`, `locations.geojson`, `historical_landslides.geojson`, `boundary.geojson` and `rainfall_daily.csv` are byte-identical to the earlier build, and `feature_version` stays `pilot-features-0.1.0`. Reloading the pilot is optional and only refreshes the `data_sources` notes.
- 2026-09-18 (v3): **Mapped inventory found.** GSI's landslide inventory is reachable on `bhusanket.gsi.gov.in` (not Bhukosh, which still times out): 8,691 surveyed records in the 8 NER states, 132 in the pilot bbox, via a public ArcGIS layer. Also loaded a CC-BY-4.0 Aizawl inventory (Zenodo 20783995, 18 dated events in the bbox). Details and every probe: `ml/reports/inventory-hunt-2026-09-18.md`. **GSI terms require permission by e-mail before anything derived from it is published** — the raw data is gitignored and not redistributed (team action).
- 2026-09-18 (v3): **Handoff changed — please reload.** `historical_landslides.geojson` now has 169 records from three sources (`gsi-bhusanket` 132, `nasa-glc` 19, `zenodo-aizawl-rsf-2026` 18), `past_landslide_density` in `grid_cells.geojson` is recomputed from the GSI surveyed inventory (1,428 of 2,798 cells > 0, max 2.23 /km²), and `feature_version` is now `pilot-features-0.2.0`. Risk scores will shift slightly after the reload because that feature feeds B0.
- 2026-09-18 (v3): Stage A retrained on the GSI labels through the **unchanged** pre-registered gate: LR 0.696 ± 0.040, RF 0.709 ± 0.041, B0 0.692 ± 0.040 (exposure-controlled spatial CV; the exposure proxy diagnostic fell to chance, 0.488). **Still no adoption** — RF fails criterion (c) and both candidates fail (e) (they flag 73-85 % of the pilot at the 70 %-recall operating point). **`b0-rules-0.1.0` remains served, interface and required features unchanged.** See `ml/reports/evaluation-2026-09-18-v3.md`.
- 2026-09-18 (v4): Stage A feature set expanded with **real, retrieved** data: 7 DEM-derived hydrology features (flow accumulation, TWI, hillslope position, plan/profile curvature, ruggedness, distance to drainage — routed over a 6 km window on Copernicus DEM GLO-90, 46 tiles, 250 MB) and 5 ISRIC SoilGrids features (clay, sand, bulk density, coarse fragments at 5-15 cm via the ISRIC WCS, plus SoilGrids-2017 depth to bedrock), all CC-BY 4.0 with missingness ≤ 0.6 % on the training rows and ≤ 0.3 % on the pilot. **Lithology was not obtained** (GLiM is CC-BY but only 0.5°; OneGeology endpoints 404/TLS-failed; GSI's geology attribute exists only at positives, so using it would leak the label). NDVI not attempted.
- 2026-09-18 (v4): Same pre-registered gate, restated not loosened. Ranking barely moved (LR 0.696 → 0.700, RF 0.709 → 0.712 pooled ROC-AUC, inside one fold std), but the **area-share problem improved materially**: RF flags 56 % of the pilot instead of 73 % at the 70 %-recall operating point, and recall at a fixed 30 % of pilot area rose 0.335 → 0.442. **Still no adoption** — criterion (e) needs ≤ 30 % and RF also fails (c). **`b0-rules-0.1.0` remains served; the handoff and `feature_version` are unchanged, so no backend reload is needed from this block.** See `ml/reports/evaluation-2026-09-18-v4.md`.
